# Local eval workspace. workspace/mock_repo is a committed fixture that
# evals.json depends on - see workspace/.gitignore for what stays untracked
# (iteration run outputs and grading logs).
workspace/

# mock_repo

This is a canned slow test suite used by the fast-tests skill eval scenarios. It is NOT meant to run — the files exist so the eval agent can inspect them via Read/Grep/Glob. The code stubs are realistic enough to support the scenario prior_contexts but are not executable.
